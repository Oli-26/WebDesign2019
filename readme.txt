Running the API:

First create a python virtual environment with version 3.7
Next activate the virtual environment
Next install the requirements for the program using "pip install -r requirements.txt"
Next populate the database by running "python populate.py"
Finally type "python run.py" to run the API.

